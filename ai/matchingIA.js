// matchingIA.js
export function computeSemanticScore(desc1, desc2) {
    // 🚨 Remplace cette fonction plus tard par un vrai appel IA
    // Ici on simule avec un score aléatoire entre 0 et 1
    return Math.random();
}
